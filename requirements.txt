# ── PyTorch (CPU build is smaller; GPU used only by Ollama, not Python) ───────
torch>=2.1.0
torchvision>=0.16.0

# ── Audio ─────────────────────────────────────────────────────────────────────
faster-whisper>=1.0.3       # replaces openai-whisper; CPU int8, 4x faster
soundfile>=0.12.1
librosa>=0.10.1
pydub>=0.25.1

# ── Vision (torchvision DenseNet121, ImageNet weights, free) ──────────────────
Pillow>=10.2.0
opencv-python-headless>=4.9.0   # headless: no GUI dep, smaller, works on servers

# ── LLM via Ollama ────────────────────────────────────────────────────────────
ollama>=0.2.1

# ── RAG ───────────────────────────────────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
faiss-cpu>=1.8.0
sentence-transformers>=2.7.0

# ── NER ───────────────────────────────────────────────────────────────────────
spacy>=3.7.4

# ── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
sse-starlette>=2.1.0
pydantic>=2.7.0

# ── Frontend ──────────────────────────────────────────────────────────────────
streamlit>=1.34.0
plotly>=5.22.0

# ── Utils ─────────────────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
requests>=2.31.0
httpx>=0.27.0
python-dotenv>=1.0.1
biopython>=1.83           # free Entrez API for PubMed abstracts

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.6
pytest-cov>=5.0.0
